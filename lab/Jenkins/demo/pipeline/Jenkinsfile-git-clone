node {
        stage('Env Variable') {
                echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
        }
	stage('Git clone') {
   		 git url: "https://github.com/ot-training/jenkins.git"
        }
}
